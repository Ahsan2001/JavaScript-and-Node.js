var guest = ["HAJRA RAFIQ", "MADIHA AHMED", "kHIZRA IQBAL"];

guest.map( (name, ind) => {
    return (
        key = ind,
        document.write( " “I would like to invite you for dinner” " + name + "<br>" )
    )
 }
)